{
  "name": "electron-shield-test",
  "version": "1.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p tsconfig.renderer.json && node scripts/copy-static.cjs",
    "start": "npm run build && electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "node node_modules/electron/install.js && printf 'Electron.app/Contents/MacOS/Electron' > node_modules/electron/path.txt"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^22.19.7",
    "electron": "^40.0.0",
    "electron-vite": "^5.0.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
